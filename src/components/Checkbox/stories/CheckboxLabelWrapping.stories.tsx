import { Checkbox } from "fish-ui-sy";

const LabelWrapping = () => (
  <Checkbox
    style={{ maxWidth: "400px" }}
    label="Here is an example of a checkbox with a long label and it starts to wrap to a second line"
  />
);

export default LabelWrapping;
